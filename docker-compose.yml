version: "3.8"

services:
  backend:
    build: ./GigFlow-Backend
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=your_atlas_uri
      - JWT_SECRET=your_secret

  frontend:
    build: ./GigFlow-Frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend